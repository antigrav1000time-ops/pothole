# Copy this file to .streamlit/secrets.toml and put your REAL key there.
# secrets.toml is what Streamlit reads automatically — never commit that
# file or share it; only this .example file (with no real key) should be
# shared/committed.
#
# Get a Groq API key from: https://console.groq.com

GROQ_API_KEY = 'Groq_API here'
